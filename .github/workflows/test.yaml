name: Deploy 

on:
 deployment:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: chrnorm/deployment-status@releases/v1
        with:
          token: "${{ github.token }}"
          state: "success"
          deployment_id: ${{ github.event.deployment.id }}
          target_url: "http://example.com/blah"
          environment_url: "http://example.com/env"
